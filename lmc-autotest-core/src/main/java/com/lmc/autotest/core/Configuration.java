package com.lmc.autotest.core;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Lazy;

public class Configuration {
//    @ConfigurationProperties("spring.datasource.autotest")
//    @Bean(name="autoTestDataSource",destroyMethod="close")
//    @Lazy
//    public DruidDataSource clickHouseDataSource(){
//        return (DruidDataSource)DataSourceBuilder.create().type(DruidDataSource.class).build();
//    }
}
